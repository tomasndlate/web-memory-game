// JS Forum

if (sessionStorage.Logged){
    // var nameOfTheUser = sessionStorage.getItem("currentUser", userName)
    var texto = document.getElementById("Language_SC").value
    console.log(texto)
}